import { WikiTopBar } from '@/components/wiki'

export const WikiTopBarDemo = {
  title: "WikiTopBar",
  component: WikiTopBar,
  description: 'A container to configure the layout of the <code>WikiSearch</code> and <code>WikiFilter</code> component in the wiki view.',
  importStatement: "import { WikiTopBar } from '@/components/wiki';",
}
